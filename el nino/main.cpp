#include <bits/stdc++.h>

using namespace std;

int main()
{
    int i,j;
    vector<int> a;
    for(i=0;i<10;i++)
    {
        cin >>j;
        a.push_back(j);
    }
    if(find(a.begin(),a.end(),9)!=a.end())
        cout << "A";
        else
        cout <<"swaru boobs small";
    return 0;
}
